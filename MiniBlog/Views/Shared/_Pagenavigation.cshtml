<nav aria-label="Page navigation">
    <ul class="pagination pagination-lg">
        @if (ViewBag.PreviewPage is not null)
        {
            <li class="page-item"> <a class="page-link" asp-r href="@ViewBag.PreviewPage">Preview</a></li>
        }
        @if (ViewBag.NextPage is not null)
        {
            <li class="page-item"> <a class="page-link" href="@ViewBag.NextPage">Next</a></li>
        }
    </ul>
</nav>